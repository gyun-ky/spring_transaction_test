spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/transaction_test
    username: root
    password: transaction_test!!
  jpa:
    hibernate:
      ddl-auto: update # validate : ???? ???? ?? ?????? ?? / none : ???? ??
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
        show_sql: true # ??????? system out? ?? ? log? ?? ??? ?
        default_batch_fetch_size: 100 # ???? ?? ??? ?? ?? ??  -> 1+N ??? ???? ?, orderItems?? where order_item.order_id in (4, 7) ????? ?? ??? ??? ??? ?????
        # in query? ??? ??? ? ?? ex) ? ???? 100??? 10 ?????? 10?? ????! in? 10?? ??? ???
        # item ?? ??? ??? ???? ex) ???? 1000?? 10? ????? 10? loop? ??  1+N? 1+1+1? ?????? ???? -> ????? redis cache ???
        use_sql_comments: true
    servlet:
      multipart:
        max-file-size: 100MB
        max-request-size: 100MB
    show-sql: true
logging:
  level:
    root: info
#    org:
#      springframework:
#        transaction:
#          interceptor: trace
    org:
      hibernate:
        sql: debug
        type:
          descriptor:
            sql: trace
      springframework:
        orm:
          jpa: debug
        transaction: debug
